// LiveKit server configuration
export const LIVEKIT_SERVER = {
  // WebRTC disabled to prevent WebSocket connection errors
  DISABLED: true,
  // WS_URL removed to prevent connection attempts
  API_KEY: 'devkey',
  API_SECRET: 'secret'
}; 